namespace Sapiscow.UnityTemplate.Module.Audios
{
    /// <summary>
    /// List all SFX here, make sure its name and this enum are same.
    /// </summary>
    public enum AudioSfx
    {
        None,
        Select,
        Confirm,
        Cancel,
        Notify
    }
}